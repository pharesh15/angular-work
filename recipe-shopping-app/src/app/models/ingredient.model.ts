export class Ingredient {
  // fields are accessible in the class with the name specified in constructor as a parameter
  constructor(public id: number, public name: string, public amount: number) {}
}
